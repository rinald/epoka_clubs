import '../utils/utils.dart';

class Club {
  const Club({this.icon, this.name, this.id, this.description});

  final IconData icon;
  final int id;
  final String name;
  final String description;
}
